apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
  name: tekton-workspace-pvc
  namespace: dev
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  volumeMode: Filesystem
  volumeName: pvc-xxxx
