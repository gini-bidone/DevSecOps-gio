apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: tekton-triggers-role
  namespace: dev
rules:
- apiGroups:
  - triggers.tekton.dev
  resources:
  - triggers
  - interceptors
  verbs:
  - get
  - list
  - watch