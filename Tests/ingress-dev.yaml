apiVersion: v1
items:
- apiVersion: networking.k8s.io/v1
  kind: Ingress
  metadata:
    name: hello-world-ingress-dev
    namespace: dev
  spec:
    rules:
    - host: mydevcluster-app.local
      http:
        paths:
        - backend:
            service:
              name: hello-world
              port:
                number: 80
          path: /
          pathType: Prefix
  status:
    loadBalancer:
      ingress:
      - hostname: localhost
kind: List
metadata:
  resourceVersion: ""
